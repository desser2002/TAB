export interface EducationType {
    _id?: string; // Опциональное поле для идентификатора
    userId: string;
    university: string;
    specialization: string;
    startYear: number;
    endYear: number;
  }
  